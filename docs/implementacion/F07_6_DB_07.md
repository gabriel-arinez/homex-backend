# F07.6 — DB-07: endurecimiento y cierre de F07

## Estado

**Cierre local verificado.**

El cierre definitivo de F07 requiere que el commit de cierre quede publicado y
que todos los jobs de GitHub Actions terminen en verde.

## Objetivo

F07.6 endurece el backend antes de iniciar F08 mediante:

- separación migrador/runtime;
- mínimo privilegio PostgreSQL;
- revisión de permisos API;
- transición real de estados de pedido;
- recorrido E2E mediante JWT/API;
- auditoría de índices;
- migraciones reproducibles;
- OpenAPI estable;
- documentación operativa;
- regresión completa de F07.

## Transiciones de pedido

Se incorporó:

```text
POST /api/v1/pedidos/{id}/cambiar-estado/
```

Estados expuestos:

- `EN_PRODUCCION`;
- `LISTO_ENTREGA`;
- `ENTREGADO`.

PostgreSQL continúa validando `transiciones_estado_pedido`. `CANCELADO`
permanece exclusivamente en la acción especializada `cancelar`.

Resultado específico:

```text
4 passed
```

## Permisos

Se verificaron:

- catálogo legible por vendedor y escritura administrativa;
- aislamiento de proformas y detalles;
- aislamiento de pedidos;
- acciones únicamente sobre pedidos autorizados;
- rechazo de acceso directo a recursos ajenos.

Resultado de permisos y transiciones:

```text
7 passed
```

## E2E JWT/API

`tests/integration/test_f076_e2e.py` utiliza autenticación JWT real.

Recorrido verificado:

```text
login
→ cliente
→ catálogo
→ proforma
→ detalle
→ envío
→ aprobación
→ pedido CONFIRMADO
→ VENTA
→ orden de trabajo
→ recibo
→ EN_PRODUCCION
→ LISTO_ENTREGA
→ nota de entrega
```

También se verificó una cancelación válida sin recibo emitido:

```text
CONFIRMADO
→ CANCELADO
→ REVERSA_VENTA
→ stock restituido
→ OT CANCELADA
```

Resultado:

```text
1 passed
```

## Índices

La migración `documentos.0002_indices_operativos_f076` fue auditada contra las
consultas reales.

La corrección se implementó mediante:

```text
documentos.0003_ajuste_indices_operativos_f076
```

sin modificar la migración ya publicada.

Índices finales relevantes:

```text
ix_productos_nombre_id
ix_proformas_vendedor_id_desc
ix_recibos_pedido_emitidos
proformas_detalle_producto_id_602f7d78
```

`EXPLAIN` confirmó su elegibilidad para catálogo, proformas por vendedor,
recibos emitidos por pedido y demanda pendiente por producto.

## Bootstrap PostgreSQL

Una PostgreSQL limpia fue reconstruida completamente mediante las migraciones
actuales.

Resultado:

```text
primera migración rc=0
segunda migración rc=0
24 tablas comerciales presentes
0 tablas faltantes
0 migraciones pendientes
```

La segunda ejecución produjo `No migrations to apply.`.

La base histórica local `homex` no se utilizó como evidencia de bootstrap y
no se empleó `--fake`.

## Privilegios PostgreSQL

Se implementaron:

- `docs/sql/privilegios_runtime.sql`;
- `scripts/verificar_privilegios_runtime.py`.

Resultado:

```text
runtime DML: OK
runtime ORM: OK
CREATE TABLE: BLOQUEADO CORRECTAMENTE
ALTER TABLE: BLOQUEADO CORRECTAMENTE
CREATE FUNCTION: BLOQUEADO CORRECTAMENTE
F07.6 privilegios PostgreSQL: OK
```

La limpieza confirmó 0 bases y 0 roles temporales restantes.

## OpenAPI

Se actualizó el contrato para:

```text
/api/v1/pedidos/{id}/cambiar-estado/
CambiarEstadoPedido
EstadoCodigoEnum
```

Resultado local:

```text
spectacular rc=0
openapi diff rc=0
```

## Gates locales

Calidad:

```text
Ruff: PASS
Ruff format: PASS
Django check: PASS
makemigrations --check --dry-run: No changes detected
git diff --check: PASS
```

Suite PostgreSQL:

```text
73 passed
```

Concurrencia PostgreSQL:

```text
9 passed
64 deselected
```

Permisos y transiciones:

```text
7 passed
```

E2E:

```text
1 passed
```

No se añadieron `skip` ni `xfail` para ocultar fallos.

## CI

El workflow contiene jobs independientes para:

- `lint`;
- `django-check`;
- `postgres-migrations`;
- `tests-postgresql`;
- `concurrency-postgresql`;
- `openapi-drift`;
- `postgres-privileges`.

F08 no debe iniciarse hasta confirmar que los gates locales y el CI remoto están
verdes.
